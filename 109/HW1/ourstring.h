#ifndef OURSTRING_H
#define OURSTRING_H
#include<stdlib.h>

//this function takes a char* and returns its length
size_t length(char *word);

//this returns the difference of two strings
//0 means no difference therefore equal
int compare( char *first, char *second);


#endif
